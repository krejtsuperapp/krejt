name: krejt_design
description: Sistemi i dizajnit i KREJT — tokenët, tema dhe komponentët e përbashkët (nga mockup-i).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
