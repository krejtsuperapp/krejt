name: krejt_push
description: Njoftimet push të KREJT-it (FCM) pas një ndërfaqeje — regjistrimi i token-it te serveri dhe hapja e ekranit të duhur.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.14.0
  firebase_messaging: ^16.6.0
  krejt_api:
    path: ../krejt_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
